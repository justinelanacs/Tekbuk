<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <!-- A single horizontal LinearLayout is enough to align everything -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="16dp">

        <!-- This LinearLayout for Name and Section takes up all available space -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvStudentName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/black"
                android:textSize="16sp"
                android:textStyle="bold"
                tools:text="Student Name" />

            <TextView
                android:id="@+id/tvSection"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/one"
                android:textSize="14sp"
                tools:text="Section" />
        </LinearLayout>

        <!-- Score TextView with margin to create space -->
        <TextView
            android:id="@+id/tvTotalScore"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:textColor="@color/one"
            android:textSize="18sp"
            android:textStyle="bold"
            tools:text="0 pts" />

        <!-- ⭐ DELETE ICON IS NOW INSIDE THE MAIN LINEARLAYOUT ⭐ -->
        <!-- It will be vertically centered automatically due to the parent's gravity -->
        <ImageView
            android:id="@+id/ivDeleteStudent"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:contentDescription="Delete Student Record"
            android:focusable="true"
            android:src="@drawable/delete"
            app:tint="@android:color/holo_red_light" />

    </LinearLayout>
</androidx.cardview.widget.CardView>
