<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:cardBackgroundColor="#B3FBF9F1"
    app:cardCornerRadius="16dp"
    app:strokeColor="@color/gray"
    app:strokeWidth="1dp"
    android:elevation="6dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Icon -->
        <ImageView
            android:id="@+id/paksaIcon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginEnd="12dp"
            android:src="@drawable/tula"
            app:tint="@color/black"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Title -->
        <TextView
            android:id="@+id/paksaTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="TITLE"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:textSize="20sp"
            app:layout_constraintStart_toEndOf="@id/paksaIcon"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/progressContainer"
            app:layout_constraintHorizontal_bias="0" />

        <!-- Description -->
        <TextView
            android:id="@+id/paksaDesc"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Description text here..."
            android:textColor="@color/black"
            android:textSize="14sp"
            app:layout_constraintTop_toBottomOf="@id/paksaTitle"
            app:layout_constraintStart_toEndOf="@id/paksaIcon"
            app:layout_constraintEnd_toStartOf="@+id/progressContainer"
            app:layout_constraintBottom_toBottomOf="parent"
            android:ellipsize="none"
            android:scrollHorizontally="false" />

        <!-- Radial Progress -->
        <FrameLayout
            android:id="@+id/progressContainer"
            android:layout_width="60dp"
            android:layout_height="60dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <com.google.android.material.progressindicator.CircularProgressIndicator
                android:id="@+id/paksaProgress"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center_vertical"
                app:indicatorSize="50dp"
                app:trackThickness="6dp"
                app:indicatorColor="@color/five"
                app:trackColor="@color/gray" />

            <TextView
                android:id="@+id/paksaPercent"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="0%"
                android:textColor="@color/black"
                android:textStyle="bold"
                android:textSize="15sp" />
        </FrameLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
